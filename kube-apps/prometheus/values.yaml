grafana:
  enabled: true

  rbac:
    pspEnabled: false

  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - grafana.dtikube.techinsights.com
  
  persistence:
    enabled: true
    size: 10Gi

prometheusOperator:
  admissionWebhooks:
    enabled: false 